import Vue from 'vue'
// 引入Vuex模块
import Vuex from 'vuex'
// 将Vuex插件作为Vue的插件注册
Vue.use(Vuex)

// 导出Vuex的Store实例
export default new Vuex.Store({
  // 状态
  state: {
  },
  // 更改
  mutations: {
  },
  // 行为
  actions: {
  }
})
